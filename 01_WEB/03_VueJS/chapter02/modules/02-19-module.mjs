let base = 100;
export const add = (x) => base + x;
export const multiply = (x) => base * x;
export default () => base;
// export { add, multiply };
// export { add, multiply };
// export default getBase;
